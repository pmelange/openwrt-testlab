targets:
  main:
    features:
      - ramboot_then_flash
      - uboot_ramboot
      - dsl_modem
      - console
      - wifi
      - online
    options:
      hostname: 'testdev02'
      lan_ip: '10.248.248.17'
      lan_netmask: '255.255.255.240'
      lan_vlan: 102
      connected_port: 'lan1'
    resources:
      - RemotePlace:
          name: port2
    drivers:
      - GpioDigitalOutputDriver:
          name: powerdriver
          bindings:
            gpio: power # resouce name
      - GpioDigitalOutputDriver:
          name: resetdriver
          bindings:
            gpio: reset # resource name
      - DigitalOutputPowerDriver:
          bindings:
            output: powerdriver
      - DigitalOutputButtonDriver:
          delay: 7.0
          bindings:
            output: resetdriver
      - SerialDriver: {}
      - TFTPProviderDriver: {}
      - UBootInteractionBoot:
          commands:
            - run fritzboot
      - UBootInteractionRamboot:
          image: "target"
          mac: "44:4e:6d:af:9c:57"
          bootpip: "192.168.0.102"
          sleep: 3
          commands:
            - bootp
            - bootm
      - OpenWrtUBootDriver:
          prompt: '\(FRITZ7530\) #'
          boot_expression: 'stop autoboot'
          boot_secret: 'a'
          boot_secret_nolf: True
      - OpenWrtShellDriver:
          keyfile: "~/.ssh/id_rsa.pub"
      - OpenWrtUciDriver:
          config:
            - set:
                network:
                  wan:
                    proto: "dhcp" # because of DSL modem
      - OpenWrtLuCIDriver: {}
      - SSHDriver:
          explicit_scp_mode: True
      - NetworkInterfaceDriver: {}
      - OpenWrtStrategy: {}
paths:
  backup: !template "$BASE/backup"
images:
  # filenames including a prefix of "/srv/tftp/testdevXX" must be 128 char or less
  target: !template $LG_IMAGE
  rescue_ramboot: "firmware/openwrt-24.10.2-ipq40xx-generic-avm_fritzbox-7530-initramfs-uImage.itb"
  restore: "backup/restore.tar.gz"
imports:
  - ../../openwrt-testlab.py
